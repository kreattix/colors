import Pallete from '../pallete'

describe('Pallete', () => {
  it('should return amber color', () => {
    expect(Pallete.amber.main).toBeTruthy()
  })
  it('should return blue color', () => {
    expect(Pallete.blue.main).toBeTruthy()
  })
  it('should return blueGray color', () => {
    expect(Pallete.blueGray.main).toBeTruthy()
  })
  it('should return brown color', () => {
    expect(Pallete.brown.main).toBeTruthy()
  })
  it('should return cyan color', () => {
    expect(Pallete.cyan.main).toBeTruthy()
  })
  it('should return deepOrange color', () => {
    expect(Pallete.deepOrange.main).toBeTruthy()
  })
  it('should return gray color', () => {
    expect(Pallete.gray.main).toBeTruthy()
  })
  it('should return green color', () => {
    expect(Pallete.green.main).toBeTruthy()
  })
  it('should return indigo color', () => {
    expect(Pallete.indigo.main).toBeTruthy()
  })
  it('should return lightBlue color', () => {
    expect(Pallete.lightBlue.main).toBeTruthy()
  })
  it('should return lightGreen color', () => {
    expect(Pallete.lightGreen.main).toBeTruthy()
  })
  it('should return lime color', () => {
    expect(Pallete.lime.main).toBeTruthy()
  })
  it('should return orange color', () => {
    expect(Pallete.orange.main).toBeTruthy()
  })
  it('should return pink color', () => {
    expect(Pallete.pink.main).toBeTruthy()
  })
  it('should return purple color', () => {
    expect(Pallete.purple.main).toBeTruthy()
  })
  it('should return deep purple color', () => {
    expect(Pallete.deepPurple.main).toBeTruthy()
  })
  it('should return red color', () => {
    expect(Pallete.red.main).toBeTruthy()
  })
  it('should return teal color', () => {
    expect(Pallete.teal.main).toBeTruthy()
  })
  it('should return yellow color', () => {
    expect(Pallete.yellow.main).toBeTruthy()
  })
})
