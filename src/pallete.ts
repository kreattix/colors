import { Color } from '.'
import { COLORS } from './enum'

class Pallete {
  static get red() {
    return Color(COLORS.red).pallete
  }
  static get pink() {
    return Color(COLORS.pink).pallete
  }
  static get purple() {
    return Color(COLORS.purple).pallete
  }
  static get deepPurple() {
    return Color(COLORS.deepPurple).pallete
  }
  static get violet() {
    return Color(COLORS.violet).pallete
  }
  static get indigo() {
    return Color(COLORS.indigo).pallete
  }
  static get blue() {
    return Color(COLORS.blue).pallete
  }
  static get lightBlue() {
    return Color(COLORS.lightBlue).pallete
  }
  static get cyan() {
    return Color(COLORS.cyan).pallete
  }
  static get teal() {
    return Color(COLORS.teal).pallete
  }
  static get green() {
    return Color(COLORS.green).pallete
  }
  static get lightGreen() {
    return Color(COLORS.lightGreen).pallete
  }
  static get lime() {
    return Color(COLORS.lime).pallete
  }
  static get yellow() {
    return Color(COLORS.yellow).pallete
  }
  static get amber() {
    return Color(COLORS.amber).pallete
  }
  static get orange() {
    return Color(COLORS.orange).pallete
  }
  static get deepOrange() {
    return Color(COLORS.deepOrange).pallete
  }
  static get brown() {
    return Color(COLORS.brown).pallete
  }
  static get gray() {
    return Color(COLORS.gray).pallete
  }
  static get blueGray() {
    return Color(COLORS.blueGray).pallete
  }
}

export default Pallete
